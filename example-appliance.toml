# Velstra Sentinel — factory-default appliance config.
#
# Minimal on purpose: the box discovers the network interfaces it actually has
# and surfaces them in `configure` (unassigned). You then assign each a zone and
# address and add firewall rules. `commit` applies changes to the running system
# live (no reboot).

[system]
hostname = "sentinel-fw"
